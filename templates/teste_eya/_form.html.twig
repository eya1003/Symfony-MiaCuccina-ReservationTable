{{ form_start(form) }}
<div class="row mb-2">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title mb-4">Basic form elements</h5>

                    <div class="form-group">
                        {{ form_widget(form.nom, {'attr': {'class':'form-control p-input' }})}}
                        {{ form_widget(form.poids)}}

                    </div>
            </div></div></div>

                    <button class="btn">{{ button_label|default('Save') }}</button>
</div>
                    {{ form_end(form) }}

