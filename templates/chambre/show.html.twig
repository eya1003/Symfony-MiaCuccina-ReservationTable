{% extends 'base.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ chambre.nom }}</td>
            </tr>
            <tr>
                <th>Nb_chaise</th>
                <td>{{ chambre.nbChaise }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chambre_index') }}">back to list</a>

    <a href="{{ path('chambre_edit', {'id': chambre.id}) }}">edit</a>

    {{ include('chambre/_delete_form.html.twig') }}
{% endblock %}
